#pragma once

// Exclude some Windows.h functionality that is not used or gets in the way
#define WIN32_LEAN_AND_MEAN				
#define NOMINMAX

#include <Windows.h>
#include <tchar.h>
#include <stdio.h>